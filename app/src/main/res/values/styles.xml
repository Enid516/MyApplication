<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowAnimationStyle">@style/AnimationTheme</item>
    </style>

    <!--Main activity theme.-->
    <style name="StartTheme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/start_window</item>
    </style>

    <!--底部对话框动画-->
    <style name="AnimationDialog" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_enter_sliding_from_bottom</item>
        <item name="android:windowExitAnimation">@anim/dialog_exit_sliding_to_bottom</item>
    </style>

    <style name="AnimationTheme" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/anim_open_in_from_right</item>
        <item name="android:activityOpenExitAnimation">@anim/anim_open_out_to_left</item>
        <item name="android:activityCloseEnterAnimation">@anim/anim_close_in_from_left</item>
        <item name="android:activityCloseExitAnimation">@anim/anim_close_out_to_right</item>
    </style>
    <style name="AnimationThemeOne">
        <item name="android:windowEnterAnimation">@anim/anim_open_in_from_right</item>
        <item name="android:windowExitAnimation">@anim/anim_close_out_to_right</item>
    </style>

</resources>
